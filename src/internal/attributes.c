//
// Created by dbernard on 2/11/20.
//

#include <signal.h>
#include <stdlib.h>
#include "essentials.h"
#include "memory.h"

//static void catch() {
////    exit(0);
//    exit(37);
//}

//CONSTRUCTOR void constructor() {
//    struct sigaction sa = (struct sigaction) {.sa_sigaction = catch, .sa_flags = SA_SIGINFO};
//
//    sigemptyset(&sa.sa_mask);
//    sigaction(SIGSEGV, &sa, NULL);
//}
